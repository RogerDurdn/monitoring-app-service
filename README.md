# Monitoring APP (service)
Shows statistics of memory usage, request response and latency in services.

- Provide a dashboard UI through static HTML
- Use an Http client to consume status endpoints
- Using as a service for config-app (user-app coming soon)
